spring:
  application:
    name: fun-config-server
  cloud:
    config:
      server:
        git:
          uri:
          search-paths:
          username:
          password: 123


logging:
  path: E:/log